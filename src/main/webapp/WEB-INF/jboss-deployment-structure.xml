<?xml version="1.0" encoding="UTF-8"?>
<!--
    Configuration WildFly :
    On exclut le module Hibernate fourni par WildFly pour utiliser
    la version embarquee dans le WAR (definie dans pom.xml).
    Cela evite les conflits de classloader entre 2 versions d'Hibernate.
    On exclut aussi le module Jakarta Mail de WildFly pour utiliser
    angus-mail bundle dans le WAR.
-->
<jboss-deployment-structure xmlns="urn:jboss:deployment-structure:1.3">
    <deployment>
        <exclusions>
            <module name="org.hibernate"/>
            <module name="org.hibernate.envers"/>
            <!-- Forcer l'utilisation d'angus-mail bundle dans le WAR -->
            <module name="jakarta.mail.api"/>
        </exclusions>
    </deployment>
</jboss-deployment-structure>
